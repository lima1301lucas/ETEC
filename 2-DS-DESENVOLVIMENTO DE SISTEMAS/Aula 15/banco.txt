create database aula; 
use aula;

create table PRODUTOS(PRDCODIGO INT AUTO_INCREMENT PRIMARY KEY,PRDNOME VARCHAR(50) NOT NULL, PRDDESCRICAO TEXT, PRDCATEGORIA INT, PRDVLRUNIT DECIMAL, PRDCODEAN VARCHAR(13));

CREATE TABLE CATEGORIAS (CTGCODIGO INT AUTO_INCREMENT PRIMARY KEY, CTGNOME VARCHAR(50) NOT NULL);

ALTER TABLE PRODUTOS ADD FOREIGN KEY(PRDCATEGORIA) REFERENCES CATEGORIAS(CTGCODIGO);

ALTER TABLE PRODUTOS ADD FOREIGN KEY(PRDCATEGORIA) REFERENCES CATEGORIAS(CTGCODIGO);