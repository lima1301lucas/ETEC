select * from ALUNOS;
select * from PARTIDOS;
select * from VOTOS;

INSERT INTO VOTOS (ELEITOR_VOTO, PARTIDO_VOTO) VALUES (06531, 2);

select ID, NOME, COUNT(0) QTDE FROM PARTIDOS left join VOTOS ON ELEITOR_VOTO=ID group by ID;


SELECT *
  FROM VOTOS;

SELECT PARTIDO_VOTO, NOME, COUNT(0) TOTAL
  FROM VOTOS
 INNER JOIN PARTIDOS ON ID = PARTIDO_VOTO
 GROUP BY PARTIDO_VOTO;




SELECT PARTIDO_VOTO, NOME, COUNT(0) TOTAL FROM VOTOS INNER JOIN PARTIDOS ON ID = PARTIDO_VOTO GROUP BY PARTIDO_VOTO;

select cast(rand()*256 as unsigned);